# GOLDEN FILE. Generated by internal/panel/golden_test.go.
# Regenerate with: bash scripts/golden-update.sh
#
# The contract of GET /status.json: every key, and the JSON type behind it.
# A key that disappears here disappears from assets/panel.js's reach, and
# the control it drives stops updating without a reload.

## fields
  connected    bool
  cutBanner    string
  cutLabel     string
  detected     string
  deviceLine   string
  devices      number
  events       null
  hasConfig    bool
  heroClass    string
  nextStep     string
  powerClass   string
  powerLabel   string
  problem      string
  running      bool
  shape        string
  trafficCut   bool
  uptime       string
  word         string

## heroClass(cut, connected, running) over its whole domain
  cut=false connected=false running=false -> off
  cut=false connected=false running=true  -> wait
  cut=false connected=true  running=false -> ok
  cut=false connected=true  running=true  -> ok
  cut=true  connected=false running=false -> cut
  cut=true  connected=false running=true  -> cut
  cut=true  connected=true  running=false -> cut
  cut=true  connected=true  running=true  -> cut

## powerClass(connected, running) over its whole domain
  connected=false running=false -> go
  connected=false running=true  -> danger
  connected=true  running=false -> ok
  connected=true  running=true  -> ok

## the value sets
  heroClass  cut off ok wait
  powerClass danger go ok
